<!--logs.wxml-->
<view class="container log-list">
  <text>登录日志</text>
  <block wx:for="{{logs}}" wx:for-item="log" wx:key="i">
    <text class="log-item">{{index + 1}}. {{log}}</text>
  </block>
</view>
